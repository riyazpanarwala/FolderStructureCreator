<Application x:Class="FolderStructureCreator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Vector Icon Geometries (16x16 standard grid) -->
            <PathGeometry x:Key="FolderGeometry" Figures="M 2,3.5 L 6.5,3.5 L 8,5.5 L 14,5.5 C 14.6,5.5 15,5.9 15,6.5 L 15,12.5 C 15,13.1 14.6,13.5 14,13.5 L 2,13.5 C 1.4,13.5 1,13.1 1,12.5 L 1,4.5 C 1,3.9 1.4,3.5 2,3.5 Z"/>
            <PathGeometry x:Key="DriveGeometry" Figures="M 2,4 C 1.4,4 1,4.4 1,5 L 1,11 C 1,11.6 1.4,12 2,12 L 14,12 C 14.6,12 15,11.6 15,11 L 15,5 C 15,4.4 14.6,4 14,4 Z M 12,8.5 A 1,1 0 1,1 12.01,8.5 M 9.5,8.5 A 1,1 0 1,1 9.51,8.5"/>
            <PathGeometry x:Key="SearchGeometry" Figures="M 6.5,2 A 4.5,4.5 0 1,0 11,6.5 A 4.5,4.5 0 0,0 6.5,2 Z M 10,10 L 14,14"/>
            <PathGeometry x:Key="SettingsGeometry" Figures="M 7,1 L 9,1 L 9.3,2.6 C 9.7,2.8 10.1,3.0 10.5,3.3 L 12,2.5 L 13.5,4 L 12.7,5.5 C 13.0,5.9 13.2,6.3 13.4,6.7 L 15,7 L 15,9 L 13.4,9.3 C 13.2,9.7 13.0,10.1 12.7,10.5 L 13.5,12 L 12,13.5 L 10.5,12.7 C 10.1,13.0 9.7,13.2 9.3,13.4 L 9,15 L 7,15 L 6.7,13.4 C 6.3,13.2 5.9,13.0 5.5,12.7 L 4,13.5 L 2.5,12 L 3.3,10.5 C 3.0,10.1 2.8,9.7 2.6,9.3 L 1,9 L 1,7 L 2.6,6.7 C 2.8,6.3 3.0,5.9 3.3,5.5 L 2.5,4 L 4,2.5 L 5.5,3.3 C 5.9,3.0 6.3,2.8 6.7,2.6 Z M 8,5.5 A 2.5,2.5 0 1,0 8,10.5 A 2.5,2.5 0 0,0 8,5.5 Z"/>
            <PathGeometry x:Key="TreeGeometry" Figures="M 2,3 L 6,3 L 6,6 L 2,6 Z M 6,9 L 10,9 L 10,12 L 6,12 Z M 4,6 L 4,10.5 L 6,10.5 M 10,13 L 14,13 L 14,16 L 10,16 Z"/>
            <PathGeometry x:Key="ChartGeometry" Figures="M 6,2 L 10,2 L 10,5 L 6,5 Z M 8,5 L 8,8 M 4,8 L 12,8 M 4,8 L 4,11 M 12,8 L 12,11 M 2,11 L 6,11 L 6,14 L 2,14 Z M 10,11 L 14,11 L 14,14 L 10,14 Z"/>
            <PathGeometry x:Key="PlusGeometry" Figures="M 8,2.5 L 8,13.5 M 2.5,8 L 13.5,8"/>
            <PathGeometry x:Key="ImportGeometry" Figures="M 8,2 L 8,10.5 M 5,7.5 L 8,10.5 L 11,7.5 M 2,10.5 L 2,13.5 L 14,13.5 L 14,10.5"/>
            <PathGeometry x:Key="ExportGeometry" Figures="M 8,9.5 L 8,1.5 M 5,4.5 L 8,1.5 L 11,4.5 M 2,7 L 2,13.5 L 14,13.5 L 14,7"/>
            <PathGeometry x:Key="TrashGeometry" Figures="M 3,4 L 13,4 M 6,4 L 6,2 L 10,2 L 10,4 M 4.5,4 L 5.2,13.5 C 5.3,14 5.6,14.5 6.2,14.5 L 9.8,14.5 C 10.4,14.5 10.7,14 10.8,13.5 L 11.5,4"/>
            <PathGeometry x:Key="PinGeometry" Figures="M 5,2 L 11,2 M 6,2 L 6,6.5 L 4,8.5 L 4,10 L 7.5,10 L 7.5,14.5 L 8.5,14.5 L 8.5,10 L 12,10 L 12,8.5 L 10,6.5 L 10,2"/>
            <PathGeometry x:Key="CloseGeometry" Figures="M 3.5,3.5 L 12.5,12.5 M 12.5,3.5 L 3.5,12.5"/>
            <PathGeometry x:Key="ChevronLeftGeometry" Figures="M 10,3.5 L 5.5,8 L 10,12.5"/>
            <PathGeometry x:Key="ChevronRightGeometry" Figures="M 6,3.5 L 10.5,8 L 6,12.5"/>
            <PathGeometry x:Key="ChevronDownGeometry" Figures="M 3.5,6 L 8,10.5 L 12.5,6"/>
            <PathGeometry x:Key="RefreshGeometry" Figures="M 13.5,3.5 L 13.5,7.5 L 9.5,7.5 M 13.2,7.5 A 5.5,5.5 0 1,0 13.5,10"/>
            <PathGeometry x:Key="SortGeometry" Figures="M 4.5,3 L 4.5,13 M 2,5.5 L 4.5,3 L 7,5.5 M 11.5,13 L 11.5,3 M 9,10.5 L 11.5,13 L 14,10.5"/>
            <PathGeometry x:Key="CommandPaletteGeometry" Figures="M 9,1.5 L 3,9 L 7.5,9 L 6.5,14.5 L 13,7 L 8.5,7 Z"/>
            <PathGeometry x:Key="RocketGeometry" Figures="M 8,1.5 C 10,3 12.5,6 12.5,9.5 L 10.5,11.5 L 8.5,10 L 6,10 L 4.5,11.5 L 2.5,9.5 C 2.5,6 5,3 8,1.5 Z M 8,5 A 1.5,1.5 0 1,1 8.01,5 M 4,12 L 2,14.5 M 12,12 L 14,14.5"/>
            <PathGeometry x:Key="DiffGeometry" Figures="M 2,2.5 L 9,2.5 L 9,9.5 L 2,9.5 Z M 7,6.5 L 14,6.5 L 14,13.5 L 7,13.5 Z"/>
            <PathGeometry x:Key="CheckGeometry" Figures="M 2.5,8 L 6.5,12 L 13.5,4"/>
            <PathGeometry x:Key="EditGeometry" Figures="M 10.5,2.5 L 13.5,5.5 L 5,14 L 2,14 L 2,11 Z"/>
            <PathGeometry x:Key="ExplorerGeometry" Figures="M 2,3 L 14,3 C 14.5,3 15,3.5 15,4 L 15,12 C 15,12.5 14.5,13 14,13 L 2,13 C 1.5,13 1,12.5 1,12 L 1,4 C 1,3.5 1.5,3 2,3 Z M 1,6 L 15,6"/>
            <PathGeometry x:Key="CameraGeometry" Figures="M 2,5 L 4,5 L 5,3 L 11,3 L 12,5 L 14,5 C 14.6,5 15,5.4 15,6 L 15,13 C 15,13.6 14.6,14 14,14 L 2,14 C 1.4,14 1,13.6 1,13 L 1,6 C 1,5.4 1.4,5 2,5 Z M 8,7 A 2.5,2.5 0 1,0 8,12 A 2.5,2.5 0 0,0 8,7 Z"/>
            <PathGeometry x:Key="ExpandGeometry" Figures="M 2,6 L 2,2 L 6,2 M 10,2 L 14,2 L 14,6 M 14,10 L 14,14 L 10,14 M 6,14 L 2,14 L 2,10"/>
            <PathGeometry x:Key="CollapseGeometry" Figures="M 6,2 L 6,6 L 2,6 M 10,2 L 10,6 L 14,6 M 14,10 L 10,10 L 10,14 M 2,10 L 6,10 L 6,14"/>
            <PathGeometry x:Key="FitGeometry" Figures="M 1,4 L 1,1 L 4,1 M 12,1 L 15,1 L 15,4 M 15,12 L 15,15 L 12,15 M 4,15 L 1,15 L 1,12"/>
            <PathGeometry x:Key="MinusGeometry" Figures="M 3,8 L 13,8"/>
            <PathGeometry x:Key="SyncGeometry" Figures="M 13.5,4 L 13.5,8 L 9.5,8 M 13.2,7.8 A 5.5,5.5 0 1,0 13.8,9.5 M 2.5,12 L 2.5,8 L 6.5,8"/>
            <PathGeometry x:Key="ArrowUpGeometry" Figures="M 8,13.5 L 8,2.5 M 3.5,7 L 8,2.5 L 12.5,7"/>
            <PathGeometry x:Key="MoreGeometry" Figures="M 3,8 A 1.2,1.2 0 1,1 3.01,8 M 8,8 A 1.2,1.2 0 1,1 8.01,8 M 13,8 A 1.2,1.2 0 1,1 13.01,8"/>

            <!-- Default Window Style -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Background" Value="{DynamicResource BrushBg}"/>
                <Setter Property="TextElement.Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            </Style>

            <!-- Keyboard Focus Visual -->
            <Style x:Key="AppFocusVisual">
                <Setter Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle Margin="-2"
                                       Stroke="{DynamicResource BrushFocusRing}"
                                       StrokeThickness="1.5"
                                       StrokeDashArray="1 1"
                                       RadiusX="6" RadiusY="6"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Standard Vector Icon Style -->
            <Style x:Key="NavIcon" TargetType="Path">
                <Setter Property="Width" Value="14"/>
                <Setter Property="Height" Value="14"/>
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="Stroke" Value="{DynamicResource BrushTextSecondary}"/>
                <Setter Property="StrokeThickness" Value="1.5"/>
                <Setter Property="StrokeStartLineCap" Value="Round"/>
                <Setter Property="StrokeEndLineCap" Value="Round"/>
                <Setter Property="StrokeLineJoin" Value="Round"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="NavIconFilled" TargetType="Path" BasedOn="{StaticResource NavIcon}">
                <Setter Property="Fill" Value="{DynamicResource BrushTextSecondary}"/>
                <Setter Property="StrokeThickness" Value="0"/>
            </Style>

            <Style x:Key="FolderIcon" TargetType="Path" BasedOn="{StaticResource NavIcon}">
                <Setter Property="Data" Value="{StaticResource FolderGeometry}"/>
                <Setter Property="Fill" Value="{DynamicResource BrushFolderIcon}"/>
                <Setter Property="Stroke" Value="{DynamicResource BrushFolderIcon}"/>
                <Setter Property="StrokeThickness" Value="0.5"/>
                <Setter Property="Width" Value="14"/>
                <Setter Property="Height" Value="14"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
            </Style>

            <!-- Toolbar Button Style -->
            <!-- Toolbar Button Style -->
            <Style x:Key="ToolButton" TargetType="Button">
                <Setter Property="Padding" Value="9,5"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
                <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushHover}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushSelected}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Compact Icon Button Style (square/circular actions) -->
            <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
                <Setter Property="Padding" Value="6"/>
                <Setter Property="MinWidth" Value="28"/>
                <Setter Property="MinHeight" Value="28"/>
                <Setter Property="Margin" Value="0,0,4,0"/>
            </Style>

            <!-- Subtle/Ghost Action Button (quiet secondary) -->
            <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushHover}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Primary Action Button (Create folders) -->
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
                <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushPrimaryBtnFg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="18,8"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushAccentHover}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushAccentHover}"/>
                        <Setter Property="Foreground" Value="{DynamicResource BrushPrimaryBtnFg}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Danger Action Button -->
            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
                <Setter Property="Foreground" Value="{DynamicResource BrushDanger}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource BrushDangerSoft}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushDanger}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Modern Themed ToolTip Style -->
            <Style TargetType="ToolTip">
                <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="HasDropShadow" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.25" Color="#000000"/>
                                </Border.Effect>
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Typography Styles -->
            <Style x:Key="AppTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            </Style>

            <Style x:Key="SectionHeader" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13.5"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            </Style>

            <Style x:Key="SubText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}"/>
            </Style>

            <Style x:Key="BadgeText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="Medium"/>
            </Style>

            <!-- Status Badge Style -->
            <Style x:Key="StatusPill" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource BrushAccentSoft}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="8,2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Input Box Styling with Accessible Placeholder -->
            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="9,6"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="Background" Value="{DynamicResource BrushInputBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource BrushAccent}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="PlaceholderText"
                                               Text="{TemplateBinding Tag}"
                                               Foreground="{DynamicResource BrushPlaceholder}"
                                               Margin="{TemplateBinding Padding}"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"
                                               FontSize="{TemplateBinding FontSize}"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- TreeViewItem Styling -->
            <Style TargetType="TreeViewItem">
                <Setter Property="Padding" Value="4,3"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TreeViewItem">
                            <StackPanel>
                                <Border x:Name="RowBorder" CornerRadius="5" Padding="4,2" Margin="0,1" SnapsToDevicePixels="True">
                                    <Grid Margin="{TemplateBinding Margin}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <ToggleButton x:Name="Expander" Grid.Column="0"
                                                      ClickMode="Press"
                                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border Background="Transparent" Width="16" Height="16">
                                                                    <Path x:Name="Arrow" Data="M 3 2 L 8 6 L 3 10 Z"
                                                                          Fill="{DynamicResource BrushTextSecondary}"
                                                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter TargetName="Arrow" Property="Data" Value="M 2 3 L 10 3 L 6 8 Z"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>
                                        <ContentPresenter x:Name="PART_Header" Grid.Column="1"
                                                          ContentSource="Header"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Grid>
                                </Border>
                                <Border x:Name="ItemsHost" Margin="8,0,0,0" Padding="12,0,0,0"
                                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1,0,0,0" Visibility="Collapsed">
                                    <ItemsPresenter/>
                                </Border>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource BrushSelected}"/>
                                </Trigger>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseDirectlyOver" Value="True"/>
                                        <Condition Property="IsSelected" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource BrushHover}"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TreeView">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="2"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <!-- ScrollBar and ScrollViewer Modern Styling -->
            <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="IsTabStop" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border x:Name="ThumbBorder"
                                    Background="{DynamicResource BrushScrollbarThumb}"
                                    CornerRadius="4"
                                    Margin="2"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource BrushScrollbarThumbHover}"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource BrushAccent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ScrollBar}">
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="10"/>
                        <Setter Property="MinHeight" Value="10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid Background="{DynamicResource BrushScrollbarTrack}">
                                        <Track x:Name="PART_Track" IsDirectionReversed="False">
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="10"/>
                        <Setter Property="MinWidth" Value="10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid Background="{DynamicResource BrushScrollbarTrack}">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ContextMenu Styling -->
            <Style TargetType="ContextMenu">
                <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="4"/>
                <Setter Property="HasDropShadow" Value="True"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.35" Color="#000000"/>
                                </Border.Effect>
                                <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ControlTemplate x:Key="SubmenuItemTemplate" TargetType="MenuItem">
                <Border x:Name="Border" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5" Padding="{TemplateBinding Padding}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ContentPresenter x:Name="IconPresenter" ContentSource="Icon"
                                              VerticalAlignment="Center" HorizontalAlignment="Center"
                                              Width="16" Height="16"/>
                            <Border x:Name="CheckBadge" Background="{DynamicResource BrushAccent}" CornerRadius="3"
                                    Width="16" Height="16" Visibility="Collapsed">
                                <Path Data="{StaticResource CheckGeometry}" Stroke="{DynamicResource BrushPrimaryBtnFg}"
                                      StrokeThickness="2" Width="10" Height="10"
                                      Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ContentPresenter Grid.Column="1" ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        <TextBlock Grid.Column="2" x:Name="InputGestureText"
                                   Text="{TemplateBinding InputGestureText}"
                                   Foreground="{DynamicResource BrushTextSecondary}"
                                   FontSize="11" Margin="16,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="Icon" Value="{x:Null}">
                        <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed"/>
                    </Trigger>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter TargetName="CheckBadge" Property="Visibility" Value="Visible"/>
                        <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed"/>
                    </Trigger>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.45"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <ControlTemplate x:Key="SubmenuHeaderTemplate" TargetType="MenuItem">
                <Border x:Name="Border" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5" Padding="{TemplateBinding Padding}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter x:Name="IconPresenter" ContentSource="Icon"
                                          Margin="0,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Center"
                                          Width="16" Height="16"/>
                        <ContentPresenter Grid.Column="1" ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        <Path Grid.Column="2" Data="{StaticResource ChevronRightGeometry}"
                              Style="{StaticResource NavIcon}" Width="8" Height="8"
                              Margin="16,0,0,0" VerticalAlignment="Center"/>
                        <Popup x:Name="SubMenuPopup" AllowsTransparency="True"
                               IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               Placement="Right" VerticalOffset="-4" HorizontalOffset="2"
                               Focusable="False" PopupAnimation="Fade">
                            <Border Background="{DynamicResource BrushPanel}"
                                    BorderBrush="{DynamicResource BrushBorder}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="4"
                                    Margin="0,0,8,8">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.35" Color="#000000"/>
                                </Border.Effect>
                                <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="Icon" Value="{x:Null}">
                        <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed"/>
                    </Trigger>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                    </Trigger>
                    <Trigger Property="IsSubmenuOpen" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.45"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style TargetType="MenuItem">
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}"/>
                <Style.Triggers>
                    <Trigger Property="Role" Value="SubmenuHeader">
                        <Setter Property="Template" Value="{StaticResource SubmenuHeaderTemplate}"/>
                    </Trigger>
                    <Trigger Property="Role" Value="TopLevelHeader">
                        <Setter Property="Template" Value="{StaticResource SubmenuHeaderTemplate}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="{x:Type Separator}">
                <Setter Property="Background" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="Margin" Value="6,4"/>
                <Setter Property="Height" Value="1"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Separator}">
                            <Rectangle Height="1" Fill="{TemplateBinding Background}" SnapsToDevicePixels="True" Opacity="0.65"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <!-- ComboBox Styling -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid x:Name="MainGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <ToggleButton x:Name="ToggleButton"
                                              Grid.ColumnSpan="2"
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="Border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>
                                <Path x:Name="Arrow"
                                      Grid.Column="1"
                                      Data="{StaticResource ChevronDownGeometry}"
                                      Stroke="{DynamicResource BrushTextSecondary}"
                                      StrokeThickness="1.5"
                                      Width="10" Height="10"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False"/>
                                <Popup x:Name="PlacementPopup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide"
                                       Placement="Bottom">
                                    <Border x:Name="DropDownBorder"
                                            MinWidth="{Binding ActualWidth, ElementName=MainGrid}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                            Background="{DynamicResource BrushDropdownBg}"
                                            BorderBrush="{DynamicResource BrushBorder}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Margin="0,4,0,0"
                                            Padding="3">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.3" Color="#000000"/>
                                        </Border.Effect>
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="5"
                                    Margin="1">
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushHover}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource BrushSelected}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
